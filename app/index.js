import { useState } from 'react';
import { View, ScrollView , SafeAreaView} from 'react-native';
import { Stack,useRouter } from 'expo-router';
import { COLORS,images, icons, SIZE } from '../constants'; 
const Home= ()=> {
      return (
        <View>
            <Text>
                Wool Mart
            </Text>
        </View>
      );
}
export default Home;

